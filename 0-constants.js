export function taskFirst()[
    const task = = 'I prefer const when I can.';
    return task;
]
 
export function getLast()
let = okay.';
return ' is okay';
 
export function taskNext()
let = But sometimes let;

combination += getLast();
return combination;

